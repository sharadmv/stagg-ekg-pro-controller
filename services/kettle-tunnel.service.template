[Unit]
Description=Cloudflare Tunnel for Kettle
After=network.target

[Service]
User={{USER}}
ExecStart=/usr/local/bin/cloudflared --config /home/sharadmv/.cloudflared/config.yml tunnel run
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
