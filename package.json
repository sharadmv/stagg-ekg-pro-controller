{
    "name": "coffee-tools",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev": "npm run dev -w @coffee-tools/landing & npm run dev -w @coffee-tools/assistant & npm run dev -w @coffee-tools/stagg",
        "build": "npm run build --workspaces --if-present",
        "lint": "npm run lint --workspaces --if-present"
    }
}